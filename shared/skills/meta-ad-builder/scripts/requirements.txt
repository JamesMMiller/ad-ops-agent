# Python dependencies for the meta-ad-builder skill.
# Install:  python3 -m pip install -r requirements.txt
requests>=2.31.0
python-dotenv>=1.0.0

# Optional — local Ad Library creative capture (enrich-ad-library-media.py):
#   python3 -m pip install playwright && playwright install chromium
# playwright>=1.40.0
